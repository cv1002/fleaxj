#![allow(dead_code)]
#![allow(unstable_name_collisions)]

/* Declare the mods that should use document */
pub mod conf;
pub mod database;
pub mod router;
pub mod serve;
/* Declare the mods that should use doctest */
pub mod util;
